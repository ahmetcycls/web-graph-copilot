import { GraphDirectiveDirective } from './graph-directive.directive';

describe('GraphDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new GraphDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
